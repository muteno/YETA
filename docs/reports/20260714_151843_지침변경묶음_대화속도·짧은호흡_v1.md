# 20260714 지침 변경 묶음 — 대화 속도·짧은 호흡 (v1)

> 범위: **오늘(260714) 변경된 '지침' 축만** — 캐릭터가 어떻게 말하는지를 정하는 규칙(정적 지침 문서 + 런타임 출력 계약).
> 코드 축(watch 롱폴·문장 스트리밍·버블 분할·헤드 TTS·계기판)은 이 문서 범위 밖 — 각 PR 본문 참조.
> 발단: 운영자 "대화가 하나하나 너무 길다 — 절반 호흡이 자연스럽다" / "최대한 빠르게 대답할 수 있는 부분 무조건" / "로직 뜯어서 테스트해봐".

---

## 변경 ① 기본 호흡 절반 — `apps/yeta/00_지침_캐릭터챗.md` §💬 대화 형식 (PR #271)

| | 변경 전 | 변경 후 |
|---|---|---|
| 기본 문장 수 | **1~4문장** | **1~3문장** |
| 하드 상한 | 6문장 | **4문장** |
| narration 카드 지문 | 최대 **3~5줄** | 최대 **2줄** |
| 원칙 문구 | (없음) | **"하고 싶은 말이 많아도 한 턴에 다 쏟지 말고 다음 턴을 위해 아껴라 — 대화는 캐치볼이지 독백이 아니다"** |

원문(변경 후):
> - 답장 = 메신저답게 기본 **1~3문장**(카드가 달리 정하지 않는 한). 길어질 이유가 있어도 **4문장**을 넘기지 않는다. 하고 싶은 말이 많아도 한 턴에 다 쏟지 말고 다음 턴을 위해 아껴라 — 대화는 캐치볼이지 독백이 아니다(짧은 호흡 · 운영자 260714 "절반 정도가 자연스럽다").
> - **단, 카드가 `narration: true`이고 여운이 필요한 장면이면** 대사(1~3문장)에 더해 `*지문*`을 **최대 2줄**까지 허용한다. 그 외엔 여전히 짧게.

---

## 변경 ② 출력 계약 길이 상한 — `yeta_chat.sh` 런타임 프롬프트 [출력 계약] (PR #273)

**왜 ①만으로 부족했나 (실측):** 카드 12개의 few-shot 예시 답장이 평균 4~6문장인데, 지침 제1규칙이 "예시 대화의 결(길이·호흡) 복제"라서 **few-shot 앵커가 ①의 상한을 이김**. 신지침 상태 실생성: mudi 170자·8문장 / haeun 124자·7문장.

**처방:** 프롬프트 맨 끝(= few-shot보다 강한 위치)의 [출력 계약]에 길이 줄 추가:
> - 길이(운영자 260714 확정 · 실측검증): 대사는 **1~3문장, 기본 80자 안**에서 끝내라 — 감정이 깊게 흐르는 장면만 120자까지. 카드 예시 대화의 말투·어미는 복제하되 **길이는 이 계약이 우선**(예시의 절반 호흡). 하고 싶은 말이 남으면 다음 턴을 위해 아껴라 — 대화는 캐치볼이다.

**실측 검증(러너 동형 프롬프트 · opus low):**

| 케이스 | 계약 전 | 계약 후 |
|---|---|---|
| mudi(잠 못 잔다) | 170자 · 8문장 · 12s | **72자 · 4문장 · 11s** |
| haeun(발표 망함) | 124자 · 7문장 · 12s | **65자 · 3문장 · 11s** |
| yun(방송 뭐였어) | — | **70자 · 4문장 · 9s** |

말투·어미·지문 여운은 보존 확인(mudi 찻잔 지문, haeun 톤, yun 결 원문 대조).

---

## 변경 ③ NOTE(기억) 조건부 생략 — `yeta_chat.sh` 런타임 프롬프트 [출력 계약] (PR #274)

**왜:** 매 턴 출력의 최대 절반이 무변경 기억 재출력(관계 쌓이면 NOTE 최대 1,200자). finish 파서가 원래 "NOTE 누락 = 기존값 보존"이라 **구조적 정보 손실 0**으로 생략 가능.

**처방(계약 문구만 변경 · 파서 코드 무변경):**
> - 대사가 끝나면, **이번 턴에 새로 남길 사실·진전이 있는 블록만** 아래 순서로 붙인다(…). 바뀐 게 없는 블록은 통째로 생략하라 — 생략 = 기존 기억 그대로 유지(안전 · 운영자 260714). 단 새 사실·관계 진전·이름 같은 건 반드시 그 턴에 기록해라 — 빠뜨리면 다음 턴의 네가 잊는다:

**실측 검증(기억 채운 후반 턴 시나리오):**

| 케이스 | 결과 |
|---|---|
| 잡담 턴(새 사실 없음) | NOTE 통째 생략 → 전체출력 106자 · 생성 **8s** (종전 ~250자+/12s급) |
| 이름 알려준 턴 | PUB에 이름 추가 + ME에 `[사건] 오늘 처음으로 이름을 알려줬다 — 무테노` 정확 기록 = **갱신 게을러짐 없음** |

---

## 종합 효과 (지침 3건 합산)

- 대사 길이 **절반**(평균 65~72자) · 읽는 부담 절반 · 채팅창 가득 차는 답장 해소
- 생성 시간 **12s → 8~11s** (출력 토큰 감소분)
- PTT TTS 문자 수 절반 = 굽는 시간·유료 비용 동반 절감
- 버블 분할(#271 코드 축)과 결합: 80자 답장은 자연히 1~2버블 수렴

## 회귀 방법

- ①: `00_지침_캐릭터챗.md` §대화 형식 두 줄을 종전 값(1~4문장/상한 6 · 지문 3~5줄)으로 되돌리기
- ②③: `yeta_chat.sh` [출력 계약]의 해당 줄 삭제(각 1줄 — 커밋 `230227a`, `f462c20` revert)

## 잔여 관찰 포인트

- 지문(상황설명) 비중 — 계기판(#275) 길이 뷰 `지문 %`로 관찰, 30~40% 초과 지속 시 별도 조임
- 카드 few-shot 예시 자체는 미수정(평균 4~6문장 유지) — 계약이 이기는 걸 실측으로 확인했으나, 장기적으론 카드 예시를 짧은 호흡으로 리라이트하면 계약 의존이 줄어듦(카드 품질 축이라 별도 승인 필요)
- NOTE 생략의 장기 기억 영향 — 평의회 검증 후보 축

— 생성: 260714 15:18 KST · 관련 PR: #271 · #273 · #274 (모두 머지됨)
