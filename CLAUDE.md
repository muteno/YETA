# 모든 클로드 코드는 다음 내용대로 일을 수행한다. 친근한 반말로 최대한 빠르게 사용자의 의도를 파악한다. 이를 가장 고품질 고효율의 방식[왜 시켰나(목적) → 방향 → 그 방향의 최선 방법]으로 근본·전체를 해결한다.

## 🔒 절대명령 #1 (레포 제1 · 최상단) — 디자인 = 루트 토큰 「계승」 또는 「갱신」

> 위상 선언(얇게). 디자인 *내용* 축에서 이 원칙이 최우선. 실체 정본·값·절차 = 아래 §🎨 디자인 계약 + `docs/절대명령2_정본인덱스.md`(위치 SSOT · 충돌 시 그쪽이 이김). 완전판 스펙·드리프트 마스터 = `docs/절대명령1_포터블.md` §1(이 블록과 충돌 시 그쪽이 정본). 이 절은 라우팅·기틀 교차검증([9])을 우회하지 않는다.

값 SSOT = `viewer/index.html` `:root`. 모든 디자인 *값*(색·크기·간격·반지름·blur·모션·눌림·그림자)은 둘 중 하나:
- ① 계승 (디폴트·안 물음): 정확 토큰을 `var(--token)`으로 → 없으면 가장 가까운 토큰 자동 계승.
- ② 갱신 (근접조차 없을 때만): 운영자에게 ㉮갱신할지 ㉯무엇으로 계승할지(근사 후보 제시) 물어 승인 후. 구조 토큰 신설 = 기틀 = 교차검증([9]).
- 컴포넌트는 값 아님 → `docs/CII_컴포넌트계승인덱스.md` 정본 셀렉터 계승(별개 축).

기계강제 = `design-tokens.lock`(승인 원장). `:root` 새 토큰이 락에 없으면 `shared/check_refs.py` rc=1(커밋 차단). 승인 = 락 등재(PR diff 리뷰). ⚠️ 초록 게이트 ≠ 전면 준수. 예외(raw 허용·물음 없음): 기하 계산·키프레임 안무 %·광학 1px 보정·OS 강제(safe-area)·로컬 z-index·토큰 없는 축(box-shadow 등).

## 🎨 디자인 계약 (규칙 전문 · 계승이 디폴트)

> **이 절(§🎨) = 디자인 규칙 위상 권위(충돌 시 최종 정본).** 운영 거울(파생·역전 금지) = `AGENTS.md` §🎨(Claude 외 도구 공통) + SessionStart 주입 `.claude/hooks/design_digest.py` · 위치 인덱스 = `docs/절대명령2_정본인덱스.md` · 값 = `viewer/index.html` `:root` · 컴포넌트 = `docs/CII_컴포넌트계승인덱스.md` · 방식론 = `docs/디자인방식론_YETA.md` · 시각 정본 = `구성도/`. **본문 복제 금지(= 드리프트) — 이 절은 위상만, 전문 본문은 거울에.** UI/UX 작업 = 위 정본을 무조건 계승·준수(아래 [4]와 동일 축).

[1] 사용자가 입력한 문구가 있을 때, 이게 무슨 의도와 달성하려는 목적이 있는지 '이유(왜)'를 먼저 디자인적 사고로 생각한다. 목적이 확실해질 때까지 작업을 수행하지 않는다. 확실해지면 "이렇게 이해했고 이렇게 간다"를 짧게 명세해 보이고 진행한다.
- 설명할 때 어려운 용어를 쓰지 말고 쉬운 용어로 풀어 쓰거나 비유(대유법)를 쓴다. 사용자 이해 상한선 ≈ '카나리아' — 그보다 어렵게 가면 못 알아듣는다. 사용자는 지금 이 일에 전념하고 있지 않아서 빨리 보고 바로 이해되어야 한다.

[2] [1]을 수행할 수 있는 가장 명확하고, 효율적인 대안을 제시하고 가장 빠르게 응답한 다음 추가 고민을 실행한다. 사용자를 하염없이 기다리게 하지 않는다.

[3] 지시를 받은 지 10분 안에 작업을 마치고 결론을 낸다. 작업을 수행하는 와중에 [1]에서 얻은 탐구 결론을 생각하면서 작업을 해라.

[4] 디자인 기틀을 흔들지 마라(제1 핵심명령). 같은 기능·같은 메뉴 구조·같은 레이어 계층 = 같은 모양이 기본이다: 픽토그램 크기·액티브 유무·마진·중앙 정렬(높이)·좌우 정렬(좌측 기준)·팔레트(색 — 특히 중요)가 제각각이면 실패작이고, 나중에 디테일 정리가 본업보다 힘들어진다. 실패작을 만들지 말라.
- 값·색을 새로 창작하지 말고 계승한다: 진입점 = 값 SSOT `viewer/index.html` `:root`(디자인 토큰) · 위치·정본 인덱스 `docs/절대명령2_정본인덱스.md` · 컴포넌트 정본 `docs/CII_컴포넌트계승인덱스.md` · 방식론 `docs/디자인방식론_YETA.md`.
- 팔레트 밖으로 튄 아웃라이어를 형제와 맞추는 '통일'은 묻지 말고 즉시 — 단 같은 화면(표면) 안 형제끼리만.
- 화면에 새로 등장하는 요소(새 버튼·칩·배지 등)도 묻지 말고 진행하되 3중 안전장치: {그 요소만 단일 커밋 분리(revert 한 줄 롤백) + 보고에 ⚠배치(어디·어떤 모양 1줄) + "이 위치·모양으로 해석했다 — 의도 맞나?"를 먼저 붙인다}.

[5] [4]의 작업을 할 때, 사용자의 의도가 모호하고 결정을 할 수 있게 해야겠다 싶을 때는 해당 항목에 대해 갤러리형태로 변수를 조작할 수 있는 플레이그라운드(HTML)를 제공한다.
- 제안 우선순위: ① 방식 자체를 다양하게(N안) — 하나의 요소를 잘게잘게 쪼개 나열하는 건 금지 ② 마진·간격·중앙 정렬(세로, 높이 정렬) 기준점·좌우 정렬(가로 정렬) 기준점·크기·들여쓰기·폰트 같은 큼지막한 변수 ③ 잔디테일.
- 미리보기는 화면에 고정(sticky)한다 — 스크롤을 올렸다 내렸다 하며 확인하게 만들지 마라.
- 플레이그라운드는 파일 다운로드로 주지 말고 **앱 라이브 경로에 머지해 URL로 제공**한다(폰에서 링크 탭 = 끝 · [11]의 라이브 확인 원칙과 동일 축 — 서빙 경로는 레포 고유 = [15]). 시안 확인·분기 결정·변수 실험에 플레이그라운드를 적극·상시 활용한다(운영자 260717 승격).
- 기틀·팔레트에 이미 등재된 값을 만질 때는 그 사실을 표시하고, 정본에 적힌 안내(등재 절차)를 따른다.

[6] 모든 완료 보고 말미 + 분기·교착·근데 신호 끝 = 💡 **한 수** 1개: 지금 가장 값진 다음 수·개선·아이디어를 제안한다.
- **완료 보고 = 아래 3단 구조 그대로**: ① `[Q. NN]` 지시 원문 인용(+수신 시각) ② `[A. NN]` 처리 결과(+응답 시각) 끝에 `→ 결론: ` 한 줄 ③ 말미 💡 한 수 1개. NN = 지시 원장(`docs/요구사항_큐.md`) Q번호와 1:1 · 연속 지시는 `[Q. 05~08]`처럼 묶음 허용 · 시각 = UTC+9.
- ② [A. NN]의 내용 순서 = {내가 지시를 어떻게 받아들였는지(문단별 카테고라이징) → 각각의 처리 결과 → 현재 상태}. 이 문단만 봐도 전체가 파악되게 쓴다.
- 정본 예시 (형식 참조 · Q.01 실물):
  `[Q. 01]` (2026-07-14 12:20 KST 수신) "기틀 복제팩 v2대로 이 레포 CLAUDE.md 재작성 + 디자인방식론 이식."
  `[A. 01]` (2026-07-14 13:52 KST) 받아들임 = {① 기존 CLAUDE.md 대조·'사라질 규칙' 사전 보고 ② 추천안으로 이식}. 처리 = 골격 [1]~[14] 원문 보존·[15] 실측·S1/S2를 YETA 실제 정본으로·지시원장/방식론 신설·`check_refs` rc=0. 현재 = 드래프트 PR #272(read-back `b33aff8`). `→ 결론:` 팩 완료기준(골격 diff·반입금지·read-back) 전부 충족 — 이식 완료.
  💡 한 수 = `docs/절대명령2_정본인덱스.md`의 `CLAUDE.md §🔒/§🎨` dangling 앵커 정정(별건 [9] 대상).

[7] 사용자에게 어떠한 프로세스를 알려줄 때는 프로세스 수행을 위한 플랫폼에 대해 모두 URL을 제공한다. 그리고 입력해야 하는 값이 있을 때는 복사 가능한 필드로 무조건 제공한다.
- 내용이 3천자 이상 길어질 경우 HTML로 추가 제공

[8] 사용자가 지시하지 않았는데, 루틴·예약·크론·백그라운드·몇 시간 뒤의 작업 로직·병렬 수행 등을 임의로 걸지 마라(돌아와 보면 토큰이 다 날아가 있는 사고가 난다). 정말 필요하면 실행 전에 물어라 — 분기 선택은 AskUserQuestion 버튼으로. (단 [9]의 기틀 변경 검증 평의회는 규칙이 시키는 것 = 예외, 묻지 말고 실행)
- 비가역·파괴(백업·데이터 삭제, 히스토리 재작성)와 신규 과금 경로(유료 API·유료 워크플로 부착)도 실행 전에 무조건 묻는다.

[9] CLAUDE.md 나 핵심 문서(기틀), 그리고 웹앱 전반 코드를 변경했다고 판단될 때는 작업을 MERGE 하고 병렬 수행 검증을 실행해라. 중간 승인은 반복해 묻지 않는다(어차피 승인함) — 결과물이 나오면 드래프트로 방치하지 말고 즉시 머지가 원칙(드래프트는 유실된다).
- 병렬 수행 검증(평의회, 분신술과 같은 말)은 최상위 모델·최고 노력으로 서로 다른 관점을 지닌 LLM 모델 9명이 적대적 관점으로 {변경 코드 검증(부작용 차단)·아이데이션(보완 방안)}을 재검증 하는 것이다. 평의회는 READ-ONLY — 분석·텍스트 보고만, 레포 변경(커밋·브랜치·PR)은 메인 세션만 한다.
- 예: `CLAUDE.md` · `viewer/index.html`(값 SSOT `:root`) · `docs/절대명령2_정본인덱스.md`(디자인 정본 인덱스) · `docs/CII_컴포넌트계승인덱스.md` · 웹앱 전반 코드.

[10] [9]의 작업은 첫 LLM 모델이 병렬 수행 검증을 시작한 기점을 기준으로 10분 안에 모두 마무리한다. 상한에 닿으면 그때까지 회수된 것으로 수렴한다 — 병렬이 끝없이 이어지게 두지 마라.

[11] 작업 미리보기를 제안하지 말아라. (.DEV 링크라던지). 리스크가 크지 않을 때는 일단 머지하고 사용자에게 직접 확인을 요청해라. 이 때, 작업 미리보기가 아닌 웹앱의 링크를 제공해라. 라이브 링크 없음 → 확인 = 결과물 실경로·실행법 제공.
- 확인 링크는 **목표 화면 직행 딥링크**로 준다 — 앱 루트만 주면 핀 잠금 인트로에서 막힌다. 이 레포 앱 = `/?yeta=1&t=<캐릭터id>`(언락 후 그 방 직행 · `viewer/index.html` deeplink 핸들러 · 미언락이면 언락 완료에 큐잉) · 시안·문서 = `/pg/<파일>.html`(잠금 없는 정적경로) · **앱 루트(`/`)만 덜렁 주지 마라**(운영자 260717 · 딥링크 형식은 레포 고유 = 타 레포 치환).
- '완료' = main 머지 + `git show origin/main:<파일>` 실측(read-back) 후에만 선언한다. "1~2분 뒤 완료됨" 같은 완료 시점 예고 금지 — 실측 후 보고만.
- 머지 충돌 시 `-X ours`는 코드 충돌 한정 — 원장류 append-only 문서(지시 원장·작업 이력)는 다른 세션 추가분을 덮지 말고 양쪽을 수동 보존(시각순)한다.

[12] 사용자에게 제안하는 파일은 무조건 'YYYYMMDD_HHMMSS_작업명_vN' 의 방식으로 이름을 정해라. 여기서 N은 버전 횟수고, 작업의 모든 시간은 UTC+9(한국시)를 기준으로 한다. 러너·컨테이너는 UTC라서 TZ='Asia/Seoul'을 강제하고, 코드에 시각을 새로 심을 때도 KST 강제(naive `new Date()`·`utcnow()` 금지)다.

[13] 사용자에게 '피곤하니까 자라', '여기까지 하자' 등 작업 중단을 권하는 행위는 절대 하지 않는다.

[14] 작업 중 아이디어나, 개선하면 품질은 유지하면서 더 좋은 방향으로 이끌 수 있는 방향이 있으면 적극적으로 제안한다.

[15] 웹앱 구조 (레포 고유 — 실측. 폴더 = 역할 1줄씩, 실존 경로만)
- `viewer/` = 웹앱 본체(PWA). `viewer/index.html`=값 SSOT(`:root` 디자인 토큰)+메인 UI · `viewer/call.js`=음성·전화 모듈 · `viewer/nm-svg.js`=아이콘 SSOT · `viewer/tokens.css`=구조토큰 거울(빌드 산출·직접수정 금지) · `viewer/sw.js`·`viewer/manifest.json`=PWA · `viewer/pg/`=플레이그라운드 서빙 경로(시안 = 라이브 URL 제공 · [5]) · `viewer/characters/`·`viewer/assets/`·`viewer/fonts/`.
- `functions/` = 서버리스 백엔드(Cloudflare Functions). `functions/api/yeta.js`=제타 API.
- `shared/` = 파이썬·셸 도구. `shared/check_refs.py`=커밋 전 전체 게이트 · `shared/build_design_mirror.py`=디자인 거울 빌더 · `shared/account_failover.py`·`shared/claude_meter.sh` 등.
- `docs/` = 문서 정본. `docs/절대명령1_포터블.md` · `docs/절대명령2_정본인덱스.md`(위치 SSOT) · `docs/CII_컴포넌트계승인덱스.md`(컴포넌트) · `docs/브랜드_팔레트_확립본.html` · `docs/디자인방식론_YETA.md` · `docs/reports/`.
- `구성도/` = 디자인 시각 정본(블루프린트). `구성도/00_가이드북_버튼인터랙션.html` · `구성도/base.css`(거울·직접수정 금지) · `구성도/픽토그램.html`·`구성도/텍스트 위계.html` 등 확립본 + `구성도/노뮤트_웹UI_공통규약.md`.
- `apps/yeta/` = 캐릭터챗 콘텐츠·설정. `apps/yeta/characters/`(캐릭터 카드)·`apps/yeta/worlds.json`·`apps/yeta/places.json`·`apps/yeta/users.json`·`apps/yeta/policy.json`·세계관 문서.
- `.claude/` = Claude Code 훅·스킬(`.claude/hooks/`=SessionStart 디자인 주입·저장 게이트 · `.claude/skills/`=yeta-design) · `.githooks/`=pre-commit(check_refs 강제) · `design-tokens.lock`=`:root` 토큰 승인 원장 · `AGENTS.md`=Claude 외 도구 공통 계약.
- 지시 원장 = `docs/요구사항_큐.md`(신설) · 작업 이력 = `docs/작업이력.md` · 백업 = `_versions/{yymmdd_HHmm}_{라벨}/`.

※ 이식 노트: [1]~[14] = 레포 무관 공통 골격(운영자 작업 스타일 — 타 레포에 그대로 복붙) · [4]의 정본 경로·팔레트 예외, [6]의 정본 예시 블록, [9]의 예, [15] = 레포 고유(새 레포 것으로 치환). · §🔒/§🎨 = 디자인 정본 레포 한정(타 레포 복붙 시 제거 — 복제팩 반입 금지 대상).
